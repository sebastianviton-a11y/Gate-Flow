/**
 * packages/types — Tipos compartidos (S0-03, esqueleto).
 *
 * Tipos compartidos entre frontend, backend y base de datos,
 * incluyendo el futuro placeholder de tipos generados desde el
 * esquema de Supabase. No depende de ningún otro paquete de GateFlow.
 *
 * Este archivo es solo el punto de entrada del esqueleto; no contiene
 * tipos de negocio todavía.
 */

export const typesLayer = "types" as const;
