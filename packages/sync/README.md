# packages/sync

Contrato de sincronización offline (interfaces TypeScript) que `apps/guard` y `packages/infrastructure` usarán para hablar de sincronización, sin depender todavía de una implementación real de PowerSync.

En Sprint 0 este paquete está vacío; el contrato se define en la tarea S0-18, bloqueada por la decisión D-06 (no se instala el SDK de PowerSync en este sprint).
