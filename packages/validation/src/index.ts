/**
 * packages/validation — Esquemas de validación (S0-03, esqueleto).
 *
 * Esquemas de validación de entradas externas. La librería específica
 * (ej. Zod) es una decisión pendiente a escalar en el momento de
 * usarse (SPRINT_00_FOUNDATION.md, alcance de packages/validation) —
 * no se asume en este sprint.
 *
 * Este archivo es solo el punto de entrada del esqueleto; no contiene
 * esquemas todavía.
 */

export const validationLayer = "validation" as const;
